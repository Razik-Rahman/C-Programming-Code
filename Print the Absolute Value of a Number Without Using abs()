#include <stdio.h>
int main() 
{
    int n,ab;
    printf("Enter the number: ");
    scanf("%d",&n);
    if (n<0) 
    {
        ab=(-n);
    } 
    else 
    {
        ab=n;
    }
    printf("The absolute value of %d is %d\n",n,ab);
    return 0;
}
