#include <stdio.h>
int main() 
{
    int a,b,m;
    printf("Enter two numbers: ");
    scanf("%d%d",&a,&b);
    m=a*(a>=b)+b*(b>a);
    printf("The maximum of two number is: %d\n",m);
    return 0;
}
