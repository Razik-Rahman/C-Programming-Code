#include <stdio.h>
#include <stdlib.h>
int main() 
{
    int a,b,m;
    printf("Enter two numbers: ");
    scanf("%d%d",&a,&b);
    m=(a+b+abs(a-b))/2;
    printf("The maximum number is: %d\n",m);
    return 0;
}
