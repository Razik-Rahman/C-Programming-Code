#include <stdio.h>
int main() 
{
    int a,b,t;
    printf("Enter two numbers: ");
    scanf("%d%d",&a,&b);
    printf("\nBefore Swapping two numbers:");
    printf("\na=%d b=%d",a,b);
    t=a;
    a=b;
    b=t;
     printf("\nAfter Swapping two numbers:");
    printf("\na=%d b=%d",a,b);
    return 0;
}
