#include <stdio.h>
int main() 
{
    int n,r,a,b;
    printf("Enter a two-digit number: ");
    scanf("%d",&n);
    b=n/10;
    a=n%10;
    r=(a*10)+b;
    printf("The reversed number is: %d\n",r);
    return 0;
}
